// Package tunnel provides the Tunnel domain service for managing
// TCP/UDP/HTTP tunnel lifecycle, routing, and traffic statistics.
//
// A Tunnel represents a persistent connection from an Agent through the
// Relay layer to the Gateway, enabling internal service exposure.
package tunnel
